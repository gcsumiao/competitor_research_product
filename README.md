# competitor_research_product
product market research dashboard based on H10 data

## Development

- Product dashboard (default): `pnpm dev`
- Template dashboard: `pnpm dev:template`

Run installs in the app folder you’re using, e.g. `pnpm -C product_dashboard install`.
